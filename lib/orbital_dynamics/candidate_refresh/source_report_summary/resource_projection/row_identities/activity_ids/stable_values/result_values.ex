defmodule OrbitalDynamics.CandidateRefresh.SourceReportSummary.ResourceProjection.RowIdentities.ActivityIds.StableValues.ResultValues do
  @moduledoc false

  def non_empty([]), do: nil
  def non_empty(ids), do: ids
end
