defmodule OrbitalDynamics.CandidateRefresh.SourceReportSummary.LinkCapacity.RoutingMaps do
  @moduledoc false

  alias __MODULE__.GroupedFields

  def fields(reports) do
    GroupedFields.fields(reports)
  end
end
