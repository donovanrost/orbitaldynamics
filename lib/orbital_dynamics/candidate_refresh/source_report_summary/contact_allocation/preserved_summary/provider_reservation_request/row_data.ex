defmodule OrbitalDynamics.CandidateRefresh.SourceReportSummary.ContactAllocation.PreservedSummary.ProviderReservationRequest.RowData do
  @moduledoc false

  alias __MODULE__.PreparedRows

  def prepare(%{} = summary) do
    PreparedRows.prepare(summary)
  end
end
