defmodule OrbitalDynamics.CandidateRefresh.SourceReportSummary.ObjectiveGap.ObjectiveSatisfaction.StatusCounts.StatusCategory.Groups.StatusSets do
  @moduledoc false

  alias __MODULE__.Categories

  def value(status), do: Categories.value(status)
end
