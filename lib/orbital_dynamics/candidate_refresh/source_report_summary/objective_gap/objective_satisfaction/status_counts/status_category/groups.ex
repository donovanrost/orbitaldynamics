defmodule OrbitalDynamics.CandidateRefresh.SourceReportSummary.ObjectiveGap.ObjectiveSatisfaction.StatusCounts.StatusCategory.Groups do
  @moduledoc false

  alias __MODULE__.StatusSets

  def value(status), do: StatusSets.value(status)
end
