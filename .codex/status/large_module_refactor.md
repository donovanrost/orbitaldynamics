# Large Module Refactor Status

Overall objective:
Reduce maintenance risk in the largest OrbitalDynamics modules and tests through
facade-preserving, responsibility-focused extraction with no public behavior,
artifact-contract, deterministic-output, or schema-export changes.

Current slice:
Operator-review row JSON Schema provider-manifest and resolver extraction.

Status:
Selected.

Selected slice:
Replace the operator-review row dependency/property dispatchers with exact lazy
provider manifests, and consolidate the identical resolver now used by all
three row schema owners into one internal helper.

Why this slice:
The operator-review row has 42 schema and 11 property pairs in the same
facade-private dispatch shape. Two proven row owners now duplicate the same
nine-line lazy resolver; adding a third copy would create avoidable maintenance
risk. One narrow internal resolver keeps lookup behavior uniform while all
provider builders remain private to `Schema`.

Public facade to preserve:
`OrbitalDynamics.Schema.json_schema/1`, `json_schema_bundle/0`, all schema
write functions, exact `cadence_import_manifest.v1` generated bytes, and
executable validation behavior.

Likely files:
- `lib/orbital_dynamics/schema.ex`
- `lib/orbital_dynamics/schema/operator_review_row_json_schema.ex`
- `lib/orbital_dynamics/schema/cadence_import_manifest_json_schema.ex`
- `lib/orbital_dynamics/schema/cadence_source_review_row_json_schema.ex`
- `lib/orbital_dynamics/schema/lazy_provider_resolver.ex`
- `.codex/status/large_module_refactor.md`

Likely verification:
- exact before/after cadence-import schema and full-bundle SHA-256
- checked-in cadence-import schema byte equality
- JSON Schema export contracts and schema export task tests
- compile, format, diff hygiene, and bounded review

Definition of done:
All 42 schema and 11 property keys/providers remain exact and lazy; both
operator-review dispatchers and both duplicate resolvers disappear; public
schema APIs and generated bytes remain unchanged; focused/export tests pass;
and bounded review finds no blocker.

Outcome:
Pending.

Verification gaps:
- Pending.

Last completed slice:
Cadence-source-review row JSON Schema provider-manifest extraction published as
`799ebda9`: all 35 schema and 13 property providers remained exact and lazy,
the cadence schema and full bundle remained byte-identical, 23/23 focused
export tests passed, and bounded review found no blocker.

Next candidate:
After this three-owner boundary is proven, reassess the remaining largest
`Schema` callback lists. Do not force provider manifests onto unrelated
validator callback registries.

Blocked:
No.
