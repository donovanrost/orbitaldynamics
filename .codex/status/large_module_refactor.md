# Large Module Refactor Status

Overall objective:
Reduce maintenance risk in the largest OrbitalDynamics modules and tests through
facade-preserving, responsibility-focused extraction with no public behavior,
artifact-contract, deterministic-output, or schema-export changes.

Current slice:
Schema capability and validation-evidence direct owner routing.

Status:
Selected.

Selected slice:
Route model-capability, validation-record/reference/check, model-acceptance, and
safety-case artifact validation directly to their existing contract owners;
remove the eight redundant facade wrappers.

Why this slice:
`schema.ex` remains the dominant in-scope production hotspot at 9,844 lines.
These clauses already have complete family-specific owners and require no
callbacks, so direct routing removes split navigation without touching
registry or JSON Schema generation.

Public facade to preserve:
`OrbitalDynamics.Schema.validate_artifact/2`, exact issue order/messages,
model-limit behavior, registry contracts, and generated schema bytes.

Likely files:
- `lib/orbital_dynamics/schema.ex`
- `.codex/status/large_module_refactor.md`

Likely verification:
- environment/model-capability and validation-evidence contract families
- exact generated schema hashes
- compile, format, diff hygiene, and bounded review

Definition of done:
All eight artifact clauses call the existing contract owners directly; issue
ordering/messages and model limits remain exact; generated schema hashes are
unchanged; focused tests pass; and bounded review finds no blocker.

Outcome:
Pending.

Verification gaps:
- Pending.

Last completed slice:
CampaignPlanner repair activity-dispatch extraction published as `ce902067`:
one owner now supplies normalization, status/protection priority, and
transition selection; 68 focused tests passed and bounded review found no
blocker.

Next candidate:
Continue schema direct-owner routing only where a whole contract family can be
removed without adding callbacks or changing generated bytes.

Blocked:
No.
