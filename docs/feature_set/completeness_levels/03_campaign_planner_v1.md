# Level 3: Campaign Planner V1

Feature complete when a fixed-horizon LEO constellation manifest can produce
ranked activity timelines, proposed contacts, warnings, assumptions, and
operator-review artifacts suitable for Cadence import.

Status: `implemented` as a transparent V1 slice. V1 now also emits a
`cadence_import_manifest.v1` over proposed contacts for artifact-only schedule
import adapters. Maturity gaps remain around resource constraints, link
capacity, schedule conflicts across shared assets, and richer objective
explanations.

